# Database
DATABASE_URL="file:./dev.db"

# JWT Secret
JWT_SECRET="dev-secret"

# AI Configuration (Optional)
# Get free API key from: https://platform.deepseek.com/
# If not set, will use fallback extraction (still works!)
DEEPSEEK_API_KEY=demo